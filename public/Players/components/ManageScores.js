import React from 'react';
import { observer } from 'mobx-react-lite';

const ManageScores = observer(function ManageScores({ store }) {

  return (
    <div>here's a div</div>
  )
});

export default ManageScores;
